services:
  service-a:
    build: ./service-a
    ports:
      - "5000:5000"
    depends_on:
      - service-b
    networks:
      - app-network

  service-b:
    build: ./service-b
    ports:
      - "5001:5001"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
