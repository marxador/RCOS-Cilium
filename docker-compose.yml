version: "3.8"

services:
  service-a:
    build: ./service-a
    ports:
      - "5000:5000"
    depends_on:
      - service-b

  service-b:
    build: ./service-b
    ports:
      - "5001:5001"
    env_file:
      - ./service-b/.env
